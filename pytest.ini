[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: marks tests that require external services like Ollama or Qdrant
addopts = -p no:cacheprovider -m "not integration"
norecursedirs = .* build dist CVS _darcs {arch} *.egg venv .venv
